{
  "name": "cleanup-plattform",
  "version": "1.0.0",
  "private": true,
  "description": "CleanUp städplattform – statisk frontend med precompilerad JSX och Tailwind.",
  "scripts": {
    "build": "node scripts/build.js",
    "config": "node scripts/generate-config.js"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-react": "^7.26.3",
    "tailwindcss": "^3.4.17"
  }
}
